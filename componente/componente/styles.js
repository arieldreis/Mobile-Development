import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    contenador: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
  },
  botao: {
    padding: 15,
    borderRadius: 8,
    alignItems: 'center',
    justifyContent: 'center',
    marginVertical: 10,
  },
});